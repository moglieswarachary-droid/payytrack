{
  "name": "credit-payment-tracker",
  "version": "1.0.0",
  "description": "Full-Stack ICICI Credit Card and Slice Payment & Outstanding Tracker App",
  "scripts": {
    "install:all": "cd server && npm install && cd ../client && npm install",
    "start": "cd server && npm start",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "dev": "node -e \"console.log('Use npm run server and npm run client, or start server after building client.')\""
  }
}
